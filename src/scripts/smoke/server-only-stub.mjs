// Stub for the Next.js "server-only" module so jiti can load server modules
// that import it. In a Next build the bundler enforces the constraint and a
// runtime shim ships with Next; outside of Next there is no real package.
// This stub is loaded by smoke scripts only.
export {};
